import 'package:flutter/material.dart';
import 'package:flutter_ta/general/music/data/song.dart';

class MusicCard extends StatelessWidget {
  const MusicCard({super.key, required this.index, required this.data});
  final int index;
  final Song data;



  @override
  Widget build(BuildContext context) {
    return GestureDetector(

    );
  }
}
